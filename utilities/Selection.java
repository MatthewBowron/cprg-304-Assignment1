package utilities;

import shapes.Shape;


public class Selection extends Sort {
    public Selection(char compareType) {
        super(compareType);
    }

    public void sort(Shape[] shapes){

    }
}